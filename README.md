# Free tools

Three self-contained HTML tools. No build step, no dependencies, no tracking.
Each page runs entirely in the visitor's browser: the Content-Security-Policy is
`default-src 'none'` with no `connect-src`, so nothing the visitor types can leave
the page even in principle.

Generated by `build_site.py` — do not edit these files directly.
